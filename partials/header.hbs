<header class="hero has-background-primary-light">
    <div class="hero-head">
    </div>
    <div class="hero-body">
        <div class="level">
            <div class="container level-right">
                <img src="{{@site.logo}}" alt="Furry Writers Guild Logo" class="image is-128x128">
                <div>
                    <h1 class="title">{{@blog.title}}</h1>
                    <h2 class="subtitle">{{@blog.description}}</h2>
                </div>
            </div>
        </div>
    </div>
    <div class="hero-footer">
    </div>
</header>
<div>
    {{navigation}}
</div>
